<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gasparaiciukas.ownhero">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroActivity" />
        <activity android:name=".CreateJournalActivity" android:windowSoftInputMode="adjustPan" />
        <activity android:name=".JournalActivity" />
        <activity android:name=".CreateStepActivity" />
        <activity android:name=".CreateGoalActivity" />
        <activity android:name=".StepActivity" />
        <activity android:name=".MotivationActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>